
import java.util.*;

public interface CallBackFunctionInterface {
	void callBackStack(Stack<jdbc> s, jdbc obj);
	void callBackList(ArrayList<jdbc> al,  jdbc obj);
	void callBackPriorityQueue(PriorityQueue<jdbc> pq,  jdbc obj);
}
